name: ci
on: [pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: greet
        run: echo "${{ github.event.pull_request.title }}"
      - run: |
          FOO=$BAR
          echo $FOO
